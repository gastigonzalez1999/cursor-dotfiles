# cursor-dotfiles

See [`AGENTS.md`](AGENTS.md) — one file, both harnesses.

The one thing to know before editing: **`skills/`, `rules/`, `commands/` and
`loop/` are generated** from `agent-dotfiles` and are replaced wholesale on every
sync. Edit them there, not here.
